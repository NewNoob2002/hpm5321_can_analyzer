# P2 24-Hour Heartbeat Schedule

- Date: 2026-08-10
- Test: `T-RTOS-003`
- Requested duration: 86,400 seconds from a fresh start
- Status: `BLOCKED_HARDWARE`
- Collector started: no

## Frozen input

- ELF: `build/p2-frozen-980a38c/demo.elf`
- Required/measured SHA-256:
  `c3feef5d36867d021e26a95548b4bd055150ca2e4be54cf907d847c8afee70d2`
- Board: `hpm5321_custom`, serial `20260723`
- Qualification probe: J-Link S/N `607000454`, JTAG 4 MHz
- Runner: `scripts/phase2/run_frozen_heartbeat.sh`

The runner now takes a non-blocking per-probe `flock`, passes the selected
probe serial explicitly, and records probe/target/JTAG identity in the JSON
evidence. A second P2 runner cannot start against the same probe lock.

## Start attempt

No J-Link/GDB collector process was active. The only enumerated SEGGER device
reported S/N `20781318`, not the qualification probe `607000454`. Commander
opened that probe and measured VTref 3.353 V, but repeated JTAG scans could not
identify `HPM5321xCFx`. The frozen ELF was therefore not programmed and the
86,400-second collector was not launched. Starting it against an unverified
probe/target would produce invalid evidence.

## Resume condition

Reconnect the qualification board/probe pair, verify S/N `607000454`, flash
`scripts/phase2/flash_p2.jlink`, and require an explicit successful target
connect/program/verify transcript. Then start from zero with:

```bash
scripts/phase2/run_frozen_heartbeat.sh
```

The board is exclusive for the full window. USB HIL, MCAN HIL, another GDB
client, or any reset invalidates the run. An interrupted run remains
`ABORTED/PARTIAL` and must restart for a full 86,400 seconds.
